import React from 'react';

const PrimaryButton = () => {
    return (
        <div>
        <button class="btn btn-primary">Get Started</button>
        </div>
    );
};

export default PrimaryButton;